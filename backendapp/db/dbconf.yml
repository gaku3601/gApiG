development:
  driver: postgres
  open: user=postgres dbname=app host=localhost port=5432 sslmode=disable

production:
  driver: postgres
  open: user=postgres dbname=app host=db port=5432 sslmode=disable
